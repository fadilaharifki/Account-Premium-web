/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["www.google.com", "unsplash.com", "images.unsplash.com", "buffer.com", "ik.imagekit.io"], // Add the required domains here
  },
};

module.exports = nextConfig;
